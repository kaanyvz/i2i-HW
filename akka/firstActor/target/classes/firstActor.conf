akka {
    actor{
        provider = remote
    }
    remote {
        artery{
            enabled = on
            transport = tcp
            canonical.hostname = "localhost"
            canonical.port = 25520
        }
    }
}